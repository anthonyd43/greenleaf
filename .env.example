# Postgres connection string (Neon in prod, local docker in dev)
DATABASE_URL=postgresql://postgres:dev@localhost:5432/postgres
# openssl rand -base64 32
AUTH_SECRET=
# Google OAuth client (console.cloud.google.com → Credentials)
AUTH_GOOGLE_ID=
AUTH_GOOGLE_SECRET=
# Email seeded as Housemate One's sign-in (your Google account email)
SEED_OWNER_EMAIL=
